To run the code execute the folllowing commands:

    go get panda
    go build
    go install

type panda --help to see all commands

to run the server:

    cd server
    go run main.go

to run the file server:

    cd server
    go run server2.go
